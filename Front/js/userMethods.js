const loadUser = () => {
	console.log("ei tá chegando aqui");
	/*const token = JSON.parse(localStorage.getItem("session")).token;
	let name = document.getElementById("name");
	Fetch.get(`http://localhost:9090/account?token=${token}`)
	.then( res => {
		console.log(res);
		name.innerHTML = res.userName;
	})
	.catch(_ => {
		window.alert("Error");
	});*/
}